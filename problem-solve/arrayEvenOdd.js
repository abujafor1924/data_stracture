/* function positiveNumbers(arr) {
  let result = [];
  for (let num of arr) {
    if (num <= 0) {
      break;
    }
    result.push(num);
  }
  return result;
}
let arr = [1, 2, 3, -4, 5, 6];
console.log(positiveNumbers(arr)); // Output: [1, 2, 3]
 */
/* 
function positiveNumbers(arr) {
  return arr.filter((num) => {
    if (num > 0) {
      return true;
    }
  });
}
let arr = [1, 2, 3, -4, 5, 6];
console.log(positiveNumbers(arr)); */
